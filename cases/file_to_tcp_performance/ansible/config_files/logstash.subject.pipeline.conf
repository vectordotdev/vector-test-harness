input {
  file {
    path => "{{ test_files_root }}*.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
}

output {
  tcp {
    host => "{{ consumer_ip }}"
    mode => "client"
    port => {{ consumer_port }}
    codec => line
  }
}
